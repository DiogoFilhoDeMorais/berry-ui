// import { createSelector } from '@ngrx/store';

// export const loginSelector = (login: any) => login;

// export const getLoginData = createSelector(
//   loginSelector,
//   (state) => (state)
// );
